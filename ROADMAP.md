# 开发路线图

## 当前版本

**v1.3.0** — 便签提醒系统 + 书签把手优化 + 贴边修复

---

## 已完成

### v1.0.0 — 首发
- 四种便签类型：今日待办、周待办、普通便签、时间轴
- 时间轴自动晋升（根据日期归类到今天/本周）
- 便签完成状态勾选
- 过期标记
- 贴边隐藏（拖拽到屏幕边缘自动隐藏为书签把手）
- 自由缩放（右下角拖拽手柄）
- 窗口置顶
- 内容搜索
- 本地持久化（JSON 文件存储）
- 无边框自定义标题栏
- 四色视觉体系

### v1.1.0
- 开机自启功能

### v1.2.0
- 系统托盘图标（右键菜单：显示/隐藏、退出）
- 关闭行为选择对话框（最小化到托盘 / 退出）
- 开机自启修复（PowerShell 直写注册表）

### v1.2.1
- 修复「最小化到托盘」无效（Electron 22 的 `showMessageBoxSync` 返回整数而非对象）

### v1.2.2
- 多行便签折叠/展开（自动折叠 + 失焦恢复）
- 贴边把手透明凸出效果（窗口透明化 + 彩色浮出把手）
- 把手点击切换分类
- 折叠摘要可直接勾选完成状态
- 修复删除按钮与折叠冲突

### v1.3.0
- **便签提醒系统**：daily/weekly/timeline 三类便签支持设置提醒
  - 到点弹出 Windows 系统通知（Toast Notification）
  - 贴边隐藏时自动弹出窗口
  - 铃铛按钮 → 弹出日期时间面板的交互方式
- **周待办提醒**：周待办便签同样支持设置提醒
- **侧边栏拖拽**：非贴边状态下可通过侧边栏空白区域拖动窗口
- **书签把手优化**：去掉圆点，高度增至 60px，布局更舒展
- **贴边状态修复**：重启后不再卡在屏幕外，坐标有效性校验

---

## 待推进

### 优先级高

1. **WebSocket 同步服务**（端口 9527）
   - 局域网内多设备实时同步便签数据
   - 基于 `ws` 库（已安装）

2. **HTTP/UDP 设备发现**
   - 自动发现局域网内其他运行片笺的设备
   - UDP 广播 + HTTP 状态查询

### 优先级中

3. **提醒功能完善**
   - 提醒列表管理面板
   - 重复提醒（每天/每周）
   - 提醒声音自定义

4. **数据导入/导出**
   - JSON 格式备份与恢复
   - 导出为 Markdown/TXT

### 优先级低

5. **Android PWA**
   - 移动端渐进式 Web 应用
   - 与桌面端同步

6. **多语言支持**
   - 英文界面
   - 主题色自定义

---

## 技术债务与注意事项

- **Electron 22**：`dialog.showMessageBoxSync` 返回整数而非 `{ response, checkboxChecked }` 对象，需用异步版
- **开机自启**：使用 PowerShell + 注册表直写方案，而非 Electron 内置 `app.setLoginItemSettings`
- **窗口透明**：`transparent: true` 开启后，贴边状态下 body 背景需设为透明
- **提醒时区**：`datetime-local` 输入值在 V8 中解析为 UTC，需手动解析日期时间各部分
- **构建体积**：便携版目标 < 100MB，当前 ~83MB
