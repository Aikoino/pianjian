# 开发路线图

## 当前版本

**v1.9.0** — 数据导入导出 + 深色模式 + 格式工具栏 + 提醒快捷预设

---

## 已完成

### v1.0.0 — 首发
- 四种便签类型：今日待办、周待办、普通便签、时间轴
- 时间轴自动晋升（根据日期归类到今天/本周）
- 便签完成状态勾选
- 过期标记
- 贴边隐藏（拖拽到屏幕边缘自动隐藏为书签把手）
- 自由缩放（右下角拖拽手柄）
- 窗口置顶
- 内容搜索
- 本地持久化（JSON 文件存储）
- 无边框自定义标题栏
- 四色视觉体系

### v1.1.0
- 开机自启功能

### v1.2.0
- 系统托盘图标（右键菜单：显示/隐藏、退出）
- 关闭行为选择对话框（最小化到托盘 / 退出）
- 开机自启修复（PowerShell 直写注册表）

### v1.2.1
- 修复「最小化到托盘」无效（Electron 22 的 `showMessageBoxSync` 返回整数而非对象）

### v1.2.2
- 多行便签折叠/展开（自动折叠 + 失焦恢复）
- 贴边把手透明凸出效果（窗口透明化 + 彩色浮出把手）
- 把手点击切换分类
- 折叠摘要可直接勾选完成状态
- 修复删除按钮与折叠冲突

### v1.3.0
- **便签提醒系统**：daily/weekly/timeline 三类便签支持设置提醒
- **周待办提醒**：周待办便签同样支持设置提醒
- **侧边栏拖拽**：非贴边状态下可通过侧边栏空白区域拖动窗口
- **书签把手优化**：去掉圆点，高度增至 60px，布局更舒展
- **贴边状态修复**：重启后不再卡在屏幕外，坐标有效性校验

### v1.4.0 — 局域网同步
- WebSocket 双向同步（端口 48484）
- UDP 广播设备发现（端口 48483）
- 手机端 Android APK（React Native + Expo）
- 冲突解决策略：发起端优先

### v1.5.0 — 同步协议升级
- 双向角色（authority/follower）冲突策略
- 下拉面板同步界面
- 合并逻辑修复

### v1.6.0 — 同步修复
- 修复手机发起配对时电脑无法连接（UDP 双向发现）
- 修复手机端 SHA256 和 crypto 兼容性问题
- 修复 WebSocket 客户端重连逻辑
- 更新检查机制（electron-updater + expo-utils）

### v1.7.0 — 功能增强
- **Markdown 渲染**：完整语法支持（标题/粗体/斜体/列表/引用/代码块/图片/链接/分割线/删除线）
- **回收站**：软删除 + 恢复 + 彻底删除 + 30天自动清除
- **重复提醒**：每天/每周/每月/每年/自定义间隔
- **便签拖拽排序**：仅普通便签，上下半区域检测，尾部拖放
- **贴边优化**：检测范围增大，支持窗口超出屏幕边缘触发
- **开机自启动修复**：更新注册表路径
- **关闭行为**：关闭窗口最小化到托盘，托盘菜单退出

### v1.8.0 — 快捷操作与安全加固
- **全部视图**：侧边栏新增「全部」标签
- **全局快捷键**：Ctrl+N 新建、Ctrl+F 搜索、Esc 关闭
- **右键菜单**：卡片右键（复制/完成/提醒/删除）、空白右键（新建四类便签）
- **自启动重构**：Electron 原生 API 替代 PowerShell
- **安全加固**：shell:openExternal 白名单、webSecurity 改用自定义协议、XSS 修复、数据校验
- **性能优化**：sync-store 缓存、sha256 提取、uuid 改用 crypto
- **Bug 修复**：回收站显示残留、计数重复、时区 off-by-one、贴边竞态、托盘显示

### v1.9.0 — 数据管理与主题
- **数据导入/导出**：JSON 备份恢复（选择性导入合并）、Markdown 导出（选择性导出）
- **格式工具栏**：10 个 Markdown 格式按钮，降低使用门槛
- **深色模式**：CSS 变量体系 + 标题栏切换 + 跟随系统主题 + 持久化
- **提醒快捷预设**：一键设置常用提醒时间 + 自定义预设管理面板
- **依赖清理**：移除未使用的 electron-updater/to-ico/png-to-ico

---

## 待推进

### 优先级高

1. **设置面板**
   - 启动行为设置
   - 字体大小调整
   - 主题色自定义

### 优先级中

2. **便签加密**

3. **自动测试**

### 优先级低

4. **Electron 升级**：从 22 升级到 28+ LTS，修复 18+ 已知安全漏洞

---

## 技术债务与注意事项

- **Electron 22**：版本过时，存在 18+ 已知安全漏洞，需升级到 28+ LTS
- **Markdown 图片**：不支持 .heif 格式，需转换为 PNG/JPEG
- **构建体积**：便携版目标 < 100MB，当前 ~85MB
