# 版本日志

## v1.7.0 (2026-06-03)

### 新增

- **Markdown 渲染**：便签内容支持完整 Markdown 语法
  - 行内：`**粗体**`、`*斜体*`、`***粗斜体***`、`` `行内代码` ``、`~~删除线~~`、`[链接](url)`、`![图片](url)`
  - 块级：`# 标题`（h1-h3）、`- 无序列表`、`1. 有序列表`、`> 引用`、围栏代码块、`---` 分割线
  - 三态切换：折叠摘要 → 展开显示（渲染 Markdown）→ 编辑（原始文本）
- **回收站**：便签删除后进入回收站，支持恢复和彻底删除
  - 侧边栏底部回收站入口，显示已删除便签数量
  - 30 天后自动清除过期回收站便签
  - 启动时自动清理过期数据
- **重复提醒**：提醒支持重复模式
  - 每天 / 每周（可选周几）/ 每月（可选日期）/ 每年 / 自定义间隔
  - 触发后自动计算下次提醒时间
  - 铃铛悬停显示重复模式
- **便签拖拽排序**：仅普通便签支持
  - 左侧 ⋮ 拖拽手柄
  - 上下半区域检测：上半部分放前面，下半部分放后面
  - 尾部拖放区域：支持拖到列表末尾
  - 拖拽时显示蓝色指示线
- **更新检查机制**
  - 桌面端：启动后检查 GitHub Releases 最新版本，蓝色横幅提示
  - 手机端：启动后检查，弹窗提示下载
- **关闭行为优化**：关闭窗口最小化到托盘，托盘菜单「退出」才真正关闭

### 优化

- **贴边检测**：检测范围从 20px 增大到 50px，支持窗口超出屏幕边缘触发
- **开机自启动**：修复注册表路径指向旧版本的问题

### 修复

- **Markdown 渲染问题**
  - 修复 `#` 标题无空格时无法识别
  - 修复代码块首行丢失
  - 修复图片/链接 `]` 和 `(` 之间空格导致无法解析
  - 修复点击链接后便签格式丢失
  - 添加 `webSecurity: false` 支持本地 file:// 图片
- **拖拽排序**
  - 修复只向上拖拽有效，向下拖拽不准确
  - 修复最后一个便签无法被拖动
- **关闭行为**：修复从任务栏关闭后无法从托盘恢复

---

## v1.3.0 (2026-05-22)

### 新增

- **便签提醒系统**：daily / weekly / timeline 三类便签支持设置提醒，到点弹出 Windows 系统通知 + 窗口自动浮出
- **铃铛弹窗交互**：卡片底部铃铛按钮点击弹出日期时间面板，智能定位避免遮挡
- **周待办提醒**：周待办便签同样支持设置提醒
- **侧边栏拖拽**：非贴边状态下可通过侧边栏空白区域拖动窗口

### 优化

- **提醒设置入口**：铃铛按钮代替常驻日期时间输入，meta 区更简洁
- **书签把手**：去掉圆点，高度增至 60px，时间轴三字不再拥挤
- **创建弹窗**：添加便签弹窗改为 div 结构，布局更灵活

### 修复

- **贴边状态卡死**：窗口贴边后重启不再卡在屏幕外，坐标有效性校验 + 非开机自启时显示在可见位置
- **窗口位置保存**：贴边状态下不保存隐藏坐标，防止下次启动位置异常
- **提醒时区解析**：手动解析日期时间字段，避免 datetime-local 的 UTC 偏移问题

---

## v1.2.2 (2026-05-22)

### 新增

- **多行便签折叠/展开**：多行便签自动折叠为单行摘要，点击展开恢复完整内容，失焦自动折叠
- **贴边把手凸出效果**：窗口透明化，贴边状态下四个彩色把手浮于屏幕边缘，悬停放大 + 阴影加深
- **贴边把手点击切换**：贴边状态下点击把手可直接切换分类视图

### 修复

- **折叠摘要复选框**：折叠状态下可直接勾选完成/未完成，无需先展开
- **删除按钮折叠冲突**：展开状态下点击删除按钮不再误触发折叠

---

## v1.2.1 (2026-05-22)

### 修复

- **关闭对话框**：修复选择「最小化到托盘」后软件仍然退出的问题。根因是 Electron 22 中 `showMessageBoxSync` 返回整数而非对象，导致 `checkboxChecked` 始终为 `undefined`，改用异步版 `showMessageBox` 修复
- **Escape 默认行为**：关闭对话框按 Escape 默认改为「最小化到托盘」而非「退出」

---

## v1.2.0 (2026-05-21)

### 新增

- **关闭行为选择**：首次点击关闭弹出对话框，可选「最小化到托盘」或「退出」，支持记住选择
- **最小化到任务栏**：最小化按钮改为标准最小化到任务栏

### 修复

- **开机自启**：改用 PowerShell + 注册表直写方案，修复开发模式下 `app.setLoginItemSettings` 不生效问题
- **托盘图标**：改用应用图标缩放，取代简陋的纯色方块
- **开机自启响应速度**：UI 乐观更新，点击即时反馈

---

## v1.1.0 (2026-05-21)

### 新增

- **开机自启**：标题栏新增开机自启开关，一键启用/关闭 Windows 开机自动启动

---

## v1.0.0 (2026-05-20)

### 首发功能

- **四种便签类型**：今日待办、周待办、普通便签、时间轴
- **时间轴自动晋升**：时间轴条目根据日期自动归类到今天/本周
- **便签完成状态**：每条便签支持勾选标记完成/未完成，完成后划线变灰
- **过期标记**：时间轴中已过日期且未完成的便签自动变灰
- **贴边隐藏**：拖拽窗口至屏幕边缘自动隐藏为彩色书签把手，悬停浮现
- **拖拽取消贴边**：贴边状态下拖拽把手即可将窗口拖出，恢复完整界面
- **自由缩放**：右下角拖拽手柄，支持自定义窗口大小
- **窗口置顶**：一键切换窗口始终在最前
- **内容搜索**：支持按关键词实时过滤便签
- **本地持久化**：所有数据自动保存至 `%APPDATA%/pianjian/data.json`
- **无边框设计**：自定义标题栏，极简视觉风格
- **四色体系**：红色（今日）、橙色（周）、绿色（便签）、粉色（时间轴）

---

> 本项目为 vibe coding 项目，由 Claude Code 辅助开发。
